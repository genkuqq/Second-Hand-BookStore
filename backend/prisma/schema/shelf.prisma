model Shelf {
  id         Int      @id @default(autoincrement())
  tag        String
  emptySpace Int      @default(80)
  books      Book[]
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
